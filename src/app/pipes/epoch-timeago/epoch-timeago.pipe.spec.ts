import { EpochTimeagoPipe } from './epoch-timeago.pipe';

describe('EpochTimeagoPipe', () => {
  it('create an instance', () => {
    const pipe = new EpochTimeagoPipe();
    expect(pipe).toBeTruthy();
  });
});
