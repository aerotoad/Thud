import { OrderByIndexPipe } from './order-by-index.pipe';

describe('OrderByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
